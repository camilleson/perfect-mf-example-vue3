{
  "private": true,
  "scripts": {
    "start": "lerna run --scope @mod-fed-vue3/* --parallel start",
    "build": "lerna run --scope @mod-fed-vue3/* build",
    "serve": "lerna run --scope @mod-fed-vue3/* --parallel serve",
    "clean": "lerna run --scope @mod-fed-vue3/* --parallel clean"
  },
  "devDependencies": {
    "lerna": "4.0.0"
  },
  "workspaces": {
    "packages": [
      "./main-app",
      "./product-detail",
      "./product-list",
      "./product-order"
    ]
  }
}
